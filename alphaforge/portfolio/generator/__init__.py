"""
portfolio/generator — Step 1–4 of the portfolio generation pipeline.

  sampler.py   — randomly sample strategy combinations
  filters.py   — correlation & conflict filters (Step 1)
  weighting.py — weight allocation methods (Step 2)
  scaler.py    — critical day detection & rescaling (Step 3)
  validator.py — drawdown validation (Step 4)
  pipeline.py  — orchestrates all steps
"""
